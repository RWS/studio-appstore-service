@model List<UserInfoModel>
@{
    ViewData["Title"] = "Manage";
    ViewData["ActivePage"] = ManageNavPages.Manage;
}

<partial name="_StatusMessage" model="@TempData["StatusMessage"]" />
<h2>@ViewData["Title"]</h2>
<h4>Manage users</h4>
<hr class="w-100">
<div class="row justify-content-center">
    <div id="alertContainer"></div>
    <partial name="_ProfileNav" />
    <div class="col-12 col-md-9">
        <h4>Users</h4>
        <table class="table table-bordered" id="usersTable">
            <thead>
                <tr class="text-center d-none d-lg-table-row">
                    <th>Username</th>
                    <th>Role</th>
                    <th></th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var user in Model)
                {
                    <tr class="text-center d-flex flex-column d-lg-table-row bg-white" id="itemsRow">
                        <td class="text-break"><p class="m-0" onclick="Collapse(this)">@user.Name</p></td>
                        <td class="align-middle px-3 d-none d-lg-table-cell">@user.Role</td>
                        @if (user.Name != "Admin")
                        {
                            <td class="align-middle px-3 d-none d-lg-table-cell" colspan="@(user.IsCurrentUser ? "2" : "1")">
                                <a asp-controller="Account" asp-action="Profile" asp-route-id="@user.Id">
                                    <i class="fa fa-pen-alt text-secondary cursor-pointer"></i>
                                </a>
                            </td>

                            @if (!user.IsCurrentUser)
                            {
                                <td class="align-middle px-3 d-none d-lg-table-cell"><i id="@user.Id" class="fa fa-trash-alt text-danger cursor-pointer delete-user-btn"></i></td>
                            }
                        }
                        else
                        {
                            <td class="align-middle px-3 d-none d-lg-table-cell" colspan="2"><i class="fa fa-info-circle text-secondary cursor-pointer" data-bs-toggle="tooltip" data-bs-placement="top" title="Built in Admin not editable!"></i> </td>
                        }
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

<partial name="_ModalPartial" model="@new ModalMessage()" />

<script src="~/js/ManageUsersScript.js"></script>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}





