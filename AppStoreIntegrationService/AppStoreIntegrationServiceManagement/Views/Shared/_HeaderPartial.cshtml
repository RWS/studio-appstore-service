@inject ISettingsRepository repository;
@{
    var siteName = (await repository.GetSettings()).Name;
}
@if (string.IsNullOrEmpty(siteName))
{
    <h2 style="color:white">PA Admin</h2>
}
else
{
    <h2 style="color:white">PA Admin - @siteName</h2>
}
